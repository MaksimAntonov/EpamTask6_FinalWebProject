package by.antonov.webproject;
/*
public enum LocalizedMessage {
  EN("messages_en"),
  RU("messages_ru");

  public String getString(String key) {
    return // value for key
  }
}

public class Factory {
  // en или ru
  public static LocalizedMessage getLocalizedMessageEnum(String locale) {
    LocalizedMessage msg = LocalizedMessage.valueOf(locale.toUpperCase());
    return msg;
  }

  public static String getLocalizedMessage(String locale, String key) {
    LocalizedMessage msg = LocalizedMessage.valueOf(locale.toUpperCase());
    return msg.getString(key);
  }
}
*/
// msg.getString(key)